plugins {
    id 'java-library'
}

apply from: "$rootDir/gradle/publish.gradle"

dependencies {
    implementation project(':exam-mq')
    api project(':env-container')
    api "com.rabbitmq:amqp-client:5.10.0"
    api "org.testcontainers:rabbitmq:$testContainers_version"
}
